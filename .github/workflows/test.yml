# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on: [push, pull_request]

# The workflow declares the minimum required permissions.
# This follows the principle of least privilege.
permissions:
  contents: read

# Cancel any in-progress runs for the same branch or PR.
# This is useful since the workflow runs on all branches.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version-file: 'go.mod'
        cache: true   # Enables module caching for faster runs.

    - name: Install golangci-lint
      # The lint target tests exercise the real binary, so it must be on PATH.
      # Pinned to the minimum version the project expects (expLintVer).
      run: |
        curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/HEAD/install.sh \
          | sh -s -- -b "$(go env GOPATH)/bin" v2.12.2

    - name: Test
      run: go test -race ./...
